# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TodoListRx' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TodoListRx
pod 'RxSwift',    '~> 4.0'
pod 'RxCocoa',    '~> 4.0'
pod 'Reusable',   '~> 3.0.0'
pod 'Then'
end
